# Lo que necesita src/app_vigencias.py para correr en Streamlit Community Cloud.
# No es el entorno completo del proyecto: la app solo lee el parquet de detalle,
# no ejecuta la liquidacion (nada de matplotlib ni scikit-learn).
# altair no se lista porque viene dentro de streamlit.
# xlsxwriter si hace falta: es el motor con que se arma el Excel del detalle
# que se descarga desde la hoja Tablas.
streamlit>=1.40
pandas>=2.0
pyarrow>=14.0
xlsxwriter>=3.1
